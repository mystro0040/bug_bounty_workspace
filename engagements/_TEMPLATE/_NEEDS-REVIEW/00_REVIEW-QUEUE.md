# 📋 Review Queue — <engagement>   (open this anytime — newest first)

What the agent needs you to review/decide, with timestamps, so you never have to scroll the chat. The
agent PREPENDS a row here the moment it parks something (and keeps working meanwhile). You review when you
can, set the Status, and tell the agent your decision. Full detail is in the matching `NN_*.md` file.

| When flagged | Status | Item file | What it needs from you |
|---|---|---|---|

_No items yet. The agent adds a row (newest first) each time it parks an operator decision._
