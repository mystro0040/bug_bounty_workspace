# 03 Scanning and Enumeration

Scan and map the attack surface — content discovery (directories, endpoints, parameters), auth flows, roles, APIs. Output: an endpoint/param inventory that drives hunting.
