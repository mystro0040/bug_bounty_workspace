# Operator profile — placeholder (PUBLIC framework copy)

The real operator profile is **private** and lives only on the bucket / private-operations side at
`global/profiles/operator.md`. This public copy is a deliberate placeholder so the framework's
initialization protocol (§2) resolves cleanly in a public checkout.

If you are running the public framework, either supply your own `global/profiles/operator.md`
describing how you want agents to work, or ignore this file — §2 treats the profile as optional.

Nothing here affects scope or the hard floor.
